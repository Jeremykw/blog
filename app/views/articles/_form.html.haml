%h1 eat my shorts
- form_for @article do |f|
	- if @article.errors.any?
		%div.error_explanation
			%h2 
				= pluralize(@article.errors.count, "error") 
				prohibited this article from being saved:
			%ul
				- @article.errors.full_messagees.each do |msg|
					%li= msg
	%p
		blaw blaw blaw
		= f.label :title
		%br
		= f.text_field :title
	%p
		= f.label :title
		%br
		= f.text_area :text
	%p
		= f.submit
